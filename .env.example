POSTGRES_USER=root
POSTGRES_PASSWORD=password
POSTGRES_DB=n8n

N8N_ENCRYPTION_KEY=super-secret-key
N8N_USER_MANAGEMENT_JWT_SECRET=even-more-secret
N8N_DEFAULT_BINARY_DATA_MODE=filesystem

# Qdrant API keys. Set at least QDRANT_API_KEY to secure external access
# via the qdrant-proxy (port 8080). You can leave the read-only key empty if
# you don't need separate read permissions.
QDRANT_API_KEY=change-me
QDRANT_READ_ONLY_API_KEY=

# For Mac users running OLLAMA locally
# See https://github.com/n8n-io/self-hosted-ai-starter-kit?tab=readme-ov-file#for-mac--apple-silicon-users
# OLLAMA_HOST=host.docker.internal:11434


